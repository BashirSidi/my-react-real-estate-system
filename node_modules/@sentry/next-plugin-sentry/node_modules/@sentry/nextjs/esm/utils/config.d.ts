import { SentryCliPluginOptions } from '@sentry/webpack-plugin';
/**
 * Next requires that plugins be tagged with the same version number as the currently-running `next.js` package, so
 * modify our `package.json` to trick it into thinking we comply. Run before the plugin is loaded at server startup.
 */
export declare function syncPluginVersionWithNextVersion(): void;
declare type WebpackConfig = {
    devtool: string;
    plugins: Array<{
        [key: string]: any;
    }>;
};
declare type NextConfigExports = {
    experimental?: {
        plugins: boolean;
    };
    plugins?: string[];
    productionBrowserSourceMaps?: boolean;
    webpack?: (config: WebpackConfig, { dev }: {
        dev: boolean;
    }) => WebpackConfig;
};
export declare function withSentryConfig(providedExports?: NextConfigExports, providedWebpackPluginOptions?: Partial<SentryCliPluginOptions>): NextConfigExports;
export {};
//# sourceMappingURL=config.d.ts.map